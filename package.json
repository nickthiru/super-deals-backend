{
  "name": "super-deals-backend",
  "version": "0.1.0",
  "bin": {
    "super-deals-backend": "bin/super-deals-backend.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "deploy": "cdk deploy --all --outputs-file outputs.json",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.155.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.155.0",
    "constructs": "^10.0.0"
  }
}